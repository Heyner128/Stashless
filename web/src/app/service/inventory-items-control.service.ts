import { Injectable } from '@angular/core';
import { Option } from '../model/option';
import { FormControl } from '@angular/forms';

@Injectable({
  providedIn: 'root'
})
export class InventoryItemsControlService {

  toFormGroup(options: Option[]) {
    
  }
}
