spring:
  security:
    oauth2:
      authorizationserver:
        issuer: http://localhost:8090/auth
        client:
          stashless:
            registration:
              redirect-uris:
                - http://localhost:4200
              post-logout-redirect-uris:
                - http://localhost:4200